# allthecodes 0.1.14

## Release Preparation

- Synchronized the Rust CLI package, Cargo lockfile, npm wrapper, and every
  platform optional-dependency alias to version `0.1.14`.
- This release build includes the current committed runtime, control-plane, and
  Rust TUI changes present on `allthecodes` before the version bump.

## Release Validation

- `cargo build --workspace --release --locked`
- Verified the release executable reports `allthecodes 0.1.14`.
- Verified `npm pack --dry-run --ignore-scripts` packages the wrapper as
  `allthecodes@0.1.14`.
